{
    "name": "word-tracker-frontend",
    "version": "1.0.0",
    "description": "Word Tracker Frontend - Angular Application",
    "scripts": {
        "install": "cd frontend && npm install",
        "build": "cd frontend && npm run build",
        "start": "npx serve -s frontend/dist/word-tracker/browser -l ${PORT:-3000}",
        "dev": "cd frontend && npm start"
    },
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=9.0.0"
    },
    "dependencies": {
        "serve": "^14.2.1"
    }
}